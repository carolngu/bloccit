New comment to "<%= @post.title %>"
<%= "=" * (@post.title.length + 12) %>

By: <%= @comment.user.name %>

<%= @comment.body %>

Visit online at: <%= topic_post_url(@topic, @post, anchor: "comment-#{@comment.id}") %>
